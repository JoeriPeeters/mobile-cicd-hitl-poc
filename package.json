{
  "name": "mobile-cicd-hitl-poc",
  "version": "0.1.0",
  "private": true,
  "description": "React Native app skeleton for a HITL CI/CD feasibility POC.",
  "scripts": {
    "test": "jest",
    "test:visual": "playwright test"
  },
  "jest": {
    "testMatch": ["**/src/**/*.test.js"]
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "jest": "^29.7.0"
  }
}
