node_modules/
*.log
.DS_Store
coverage/

# Mobile build artifacts (once real builds are wired in)
ios/build/
android/app/build/
*.ipa
*.aab
*.apk

# Expo web export (generated by `npm run export:web`; what the visual test builds in CI)
mobile/dist/
# Generated lockfile — CI runs `npm install`, so it isn't pinned here
mobile/package-lock.json
